define (
    [
        "./ui/inbox",
        "./ui/outbox",
        "./ui/composition"
    ],
    function (Inbox, Outbox, Composition) {
        function initialize () {

        };
        return {initialize: initialize}
    }
);