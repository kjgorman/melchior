define (
    [
        "./ui/inbox",
        "./ui/outbox"
    ],
    function (Inbox, Outbox) {

        var initialize = function () {
            //do some logic connection
        };
        return { initialize: initialize }
    }
);